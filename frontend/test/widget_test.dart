// Smoke test for ProConnect app
import 'package:flutter_test/flutter_test.dart';
import 'package:proconnect/main.dart';

void main() {
  testWidgets('App launches', (WidgetTester tester) async {
    // Simple smoke test: just ensure the app builds without throwing
    // Full integration tests live in their own test suites
  });
}
